// had an issue with rerouting to subsequent pages for onboarding and
// this was the solution I came up with...might fix later if needed
export enum NextScreen {
    PickWorkoutBuddy = "pickworkoutbuddy",
    Gender = "gender",
}